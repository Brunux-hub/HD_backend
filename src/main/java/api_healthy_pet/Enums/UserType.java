package api_healthy_pet.Enums;

public enum UserType {
    ADMIN,
    VETERINARIAN,
    RECEPTIONIST,
    CLIENT,
    WORKER // Solo para compatibilidad con datos existentes. No usar en nuevos registros.
}
