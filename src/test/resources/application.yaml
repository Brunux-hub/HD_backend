spring:
  datasource:
    url: jdbc:h2:mem:healthypets;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS healthy_pets_bd_2
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: healthy_pets_bd_2
jwt:
  secret: Q2FtYmlhclBvclVuU2VjcmV0b1NlZ3Vyb0RlMzJCeXRlc01pbmltbw==
  expiration: 3600000

app:
  cors:
    allowed-origins: http://localhost:5173,http://localhost:3000
